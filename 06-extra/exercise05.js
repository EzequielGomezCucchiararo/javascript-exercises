// isString

// Define a function called isString that receives a value and return
// true if the value received is a string

function isString (s) { // eslint-disable-line no-unused-vars
  return typeof s === 'string'
}

isString('a')
