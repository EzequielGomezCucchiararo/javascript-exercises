// isNumber

// Define a function called isNumber that receives a value and return true
// if the value received is a number

function isNumber (x) { // eslint-disable-line no-unused-vars
  return typeof x === 'number'
}

isNumber(2)
