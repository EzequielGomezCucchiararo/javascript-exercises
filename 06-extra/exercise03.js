// getFullName

// Define a function called getFullName that receives a name and a surname and
// return the string "Your full name is " with the name & surname concatenated

function getFullName (sname, ssurname) { // eslint-disable-line no-unused-vars
  return 'Your full name is ' + sname + ' ' + ssurname
}

getFullName('Ezequiel', 'Gómez')
